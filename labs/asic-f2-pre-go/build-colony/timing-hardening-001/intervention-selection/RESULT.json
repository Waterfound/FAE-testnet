{
  "domain_id": "intervention-selection",
  "outcome": "built",
  "source_revision": "07c7b03237b2eaa59c0252bd85b8a2e16769c706",
  "selected_candidate": {
    "id": "C1-RW5-REGFILE-COMPLETE-PARTITION",
    "scope": "labs/asic-f2-pre-go/hls/fae_dp6_hls.cpp",
    "intended_diff": [
      "Add HLS complete ARRAY_PARTITION for rw5 parameter r[8].",
      "Add matching HLS complete ARRAY_PARTITION for dp6_run_bytes local regs[8].",
      "Do not change any C/C++ expression, branch, memory address, constant, loop bound, function signature, or AWS interface."
    ],
    "rationale": [
      "Vitis binding evidence shows dynamic r[] accesses are implemented as RAM, Width=64, Depth=8, Latency=1.",
      "Routed timing evidence repeatedly identifies regs_U/ram_reg_bram_* in worst setup paths across baseline and both valid P&R variants.",
      "The object is only 8x64 bits, so complete partition is substantially smaller in scope than modifying Argon2 fill_block or Blamka arithmetic.",
      "The intervention changes storage realization only; exact semantic equivalence is still fail-closed by full-output and multi-vector parity."
    ]
  },
  "rejection_conditions": [
    "Any canonical 160-byte mismatch.",
    "Any failure among the 16 parity vectors.",
    "Vitis rejects the partition directive or silently retains the 8x64 r[] object as the same RAM implementation.",
    "csynth/resource tractability materially regresses without a compensating timing rationale.",
    "Full routed timing remains negative or hold becomes negative."
  ],
  "fallback_order": [
    "If directive syntax/binding is ineffective, adjust only the legal placement of the same partition directive and re-verify semantics.",
    "If C1 is semantically clean but physically ineffective, target Argon2 fill_block local r/t storage with bounded partition/reshape experiments.",
    "Only after bounded storage interventions fail consider explicit Blamka/DSP operator-latency hardening.",
    "Do not escalate to algorithmic or consensus changes."
  ],
  "promotion_gate": "C1 may enter localized-hls-patch only after independent verifier accepts this selection."
}
