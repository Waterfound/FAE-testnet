# Versioned schemas

These schemas describe the portable v0.0.1 artifacts. Runtime validation in src/contracts.mjs additionally enforces security invariants such as executable allowlists, offline intent, serial execution, clean Git targets and the non-authority boundary.

Schema compatibility does not imply permission to execute an unreviewed campaign.
